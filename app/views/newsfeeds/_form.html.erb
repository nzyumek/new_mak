<%= form_with(model: newsfeed, local: true) do |form| %>
  <% if newsfeed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(newsfeed.errors.count, "error") %> prohibited this newsfeed from being saved:</h2>

      <ul>
        <% newsfeed.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>
  
  <div class="field">
    <%= form.file_field :image, accept: "image/png, image/jpeg, image/gif"%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
